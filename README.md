# Media Downloader Telegram Bot

Universal Telegram Bot for downloading media from Instagram and Telegram with an innovative pairing system.

## Setup
1. Clone the repository
2. Run `poetry install`
3. Copy `.env.example` to `.env` and fill the keys.
4. Run the bot.

See `prd.md` and `roadmap.md` for project details.
